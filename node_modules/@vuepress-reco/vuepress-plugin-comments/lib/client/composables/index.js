import { computed } from 'vue';
import { useThemeLocaleData } from '@vuepress/plugin-theme-data/client';
export function useComment() {
    const themeLocal = useThemeLocaleData();
    const solution = computed(() => {
        var _a;
        // @ts-ignore
        switch ((_a = themeLocal.value.commentConfig) === null || _a === void 0 ? void 0 : _a.type) {
            case 'valine':
                return 'valine';
            case 'waline':
                return 'waline';
            case 'giscus':
                return 'giscus';
            default:
                return '';
        }
    });
    const options = computed(() => {
        var _a, _b;
        // @ts-ignore
        return ((_b = (_a = themeLocal.value) === null || _a === void 0 ? void 0 : _a.commentConfig) === null || _b === void 0 ? void 0 : _b.options) || {};
    });
    return { solution, options };
}
