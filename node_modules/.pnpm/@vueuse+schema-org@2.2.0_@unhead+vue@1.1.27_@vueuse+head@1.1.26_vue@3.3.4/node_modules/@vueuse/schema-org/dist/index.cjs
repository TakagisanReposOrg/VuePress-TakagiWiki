'use strict';

const schemaOrgVue = require('@unhead/schema-org-vue');



Object.keys(schemaOrgVue).forEach(function (k) {
	if (k !== 'default' && !exports.hasOwnProperty(k)) exports[k] = schemaOrgVue[k];
});
